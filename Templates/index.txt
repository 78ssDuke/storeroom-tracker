<!DOCTYPE html>
<html>
<head>
    <title>Storeroom Inventory</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
</head>
<body class="container mt-4">

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Storeroom Inventory</h1>
    <a href="/logout" class="btn btn-danger">Logout</a>
</div>

<form action="/add" method="post" class="form-inline mb-3">
    <input name="name" class="form-control mr-2" placeholder="Item Name" required>
    <input name="quantity" class="form-control mr-2" type="number" placeholder="Qty" required>
    <input name="location" class="form-control mr-2" placeholder="Location">
    <button class="btn btn-primary">Add</button>
</form>

<table class="table table-bordered table-striped table-sm">
    <thead class="thead-dark">
        <tr>
            <th>Item</th>
            <th>Quantity</th>
            <th>Location</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        {% for item in items %}
        <tr>
            <td>{{ item.name }}</td>
            <td>
                <form action="/update/{{ item.id }}" method="post" class="form-inline">
                    <input name="quantity" type="number" value="{{ item.quantity }}" 
                           class="form-control mr-2" style="width:80px;">
                    <button class="btn btn-sm btn-secondary">Save</button>
                </form>
            </td>
            <td>{{ item.location }}</td>
            <td>
                <a href="/delete/{{ item.id }}" class="btn btn-sm btn-danger">Delete</a>
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="4" class="text-center">No items in inventory</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

</body>
</html>